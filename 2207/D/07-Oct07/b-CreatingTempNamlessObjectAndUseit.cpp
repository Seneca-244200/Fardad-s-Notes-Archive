#include "Account.h"
using namespace sdds;
int main() {
   //Account checking(654321,100);
   //checking.display();
   //checking.display();
   //checking.depost(300.0);
   //checking.display();
   //// checking.m_balance += 200; not allowed.
   //checking.depost(200.0);
   //checking.display();

   Account(98765, 10000).display();
   return 0;
}