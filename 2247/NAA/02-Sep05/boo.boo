 if ( age < 14 ) {
      cout << "Wow too young to be in college!" << endl;
   }
   else {
      cout << "Take a seat!" << endl;
   }
   cout << "Enter a character\n> ";
   cin >> ch;
   cout << "*" << ch << "*" << endl;
   return 0;
}