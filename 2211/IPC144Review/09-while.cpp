#include <cstdio>
using namespace std;

int main() {
   int done = 0;
   // happens zero or many times
   while (!done) {

   }
   // happens at least once.
   done = 0; 
   do {

   } while (!done);


   for (first1, first2; cond; last1, last2) {
      body;
   }

   first1;
   first2;

   while (cond) {
      body;


      last1;
      last2;
   }

   return 0;
}