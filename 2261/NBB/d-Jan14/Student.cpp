#include "Student.h"
int passingGrade{ 50 };// global because of the extern in Student.h
int noOfsubjects{}; // file scope visible to all file
namespace seneca {
   void reg(const Student* S) {

   }

}