#include <iostream>
using namespace std;
bool duplicate_string(const char* source, char*& destination);
int main() {
   cout << "OOP244 NAA - JAN23" << endl;
   return 0;
}

bool duplicate_string(const char* source, char*& destination){
   // if source exists
   // allocate new memory to the size of source 
   // copy the data into new memory
   // make sure destination is free to use, if not free it and make it reade
   // make desitation point to the newly allocated memory

   return theResult;
}
