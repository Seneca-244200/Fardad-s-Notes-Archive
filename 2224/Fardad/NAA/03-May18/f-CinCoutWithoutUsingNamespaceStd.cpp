#include <iostream>
// cout global object that is an instance of ostream
// cin global object thati is an isntance of istream 
int main() {
   int  // class 
      i = 0;  // instance(object) of the class int that is initialized to zero;
   double  // class
      j;    // instance(object) of the class double that is not initialized;
   j = 0;   // j is set to zero;

   std::cout << "Hi my name is Fred Soley and I am " << 40 << " years old!" << std::endl;
   return 0;
}