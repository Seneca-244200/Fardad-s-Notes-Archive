#include <iostream>
#include <fstream>
using namespace std;

template <typename type>
type add(type a, type b) {
   return a + b;
}

int main() {
   double a, b, c;
   int i, j, k;
   a = add(b, c);
   i = add(j, k);

   return 0;
}