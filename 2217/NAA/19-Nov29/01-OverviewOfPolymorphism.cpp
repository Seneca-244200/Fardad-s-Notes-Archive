/*
Polymorphism  // doing the same action in differnt ways
   Ad-Hoc   // fardad's name: fake polymorphism
      Coercion   // C or C++
         int a, b;
         double c, d;
         a = b;        integer into an integer
         a = c; // a = int(c);   double casted to an integer and then put into an integer
      Overloading
         void display(int a);
              // behind the scene display_int
         void display(const char* str);
              // behind the scene display_const_chr_*
   Universal   // the real polymorphism, the correct action will be taken
               // based on the type of the object
      Inclusion
          // Virtual functions
      Parametric
          // templates









*/


#include <iostream>
using namespace std;
int main() {
   return 0;
}
