#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main() {
   int done = 0;
   while (!done) {
      if (a > 10) {
         //watever 
         done = 1;
      }
      else if () {
         done = 1;
      }
      if (!done && b > 30) {
         if (cond)
            done = 1;
      }
   }



   return 0;
}