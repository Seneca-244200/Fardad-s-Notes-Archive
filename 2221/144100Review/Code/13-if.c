#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
   int a = -10;
   if (a) {  // false is only ZERO everything else is true
      // one
   }
   else {
      // two
   }

   // one or no selection out of many
   if () {

   }
   else if () {

   }
   else if () {

   }
   // one of many
   if (a > 2) {

   }
   else if (b < 5) {

   }
   else if (str[0] == 'a') {

   }
   else {

   }
   return 0;
}